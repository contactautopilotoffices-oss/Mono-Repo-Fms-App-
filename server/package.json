{
  "name": "autopilot-server",
  "version": "1.0.0",
  "description": "Autopilot Mobile — Fastify QA Server + Mock APIs",
  "main": "dist/index.js",
  "scripts": {
    "dev": "node --env-file=.env -r tsx src/index.ts",
    "build": "tsc",
    "start": "node -r dotenv/config dist/index.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@fastify/cors": "^9.0.1",
    "@supabase/supabase-js": "^2.45.4",
    "dotenv": "^16.4.5",
    "fastify": "^4.28.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "supertest": "^7.0.0",
    "@types/supertest": "^6.0.2",
    "tsx": "^4.16.2",
    "typescript": "^5.5.3",
    "vitest": "^2.0.3"
  }
}
